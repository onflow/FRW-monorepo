import { fn } from 'storybook/test';

// These are the mock function instances, exported with the names the component expects.
export const useNftCatalogCollections = fn().mockName('useNftCatalogCollections');
