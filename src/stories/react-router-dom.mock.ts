import { fn } from 'storybook/test';

export const Link = fn().mockName('Link');
export const Navigate = fn().mockName('Navigate');
export const useNavigate = fn().mockName('useNavigate');
export const useHistory = fn().mockName('useHistory'); // Keep for backward compatibility
