{
  "name": "@onflow/frw-services",
  "version": "0.0.1",
  "description": "Package for flow wallet services",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "source": "./src/index.ts",
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "build:watch": "tsup --watch",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist",
    "test": "vitest",
    "test:run": "vitest --run",
    "test:coverage": "vitest --coverage",
    "prepublishOnly": "pnpm build",
    "lint": "eslint src --cache",
    "lint:fix": "eslint src --cache --fix"
  },
  "exports": {
    ".": {
      "@onflow/source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "@babel/runtime": "^7.28.2",
    "@onflow/frw-api": "workspace:*",
    "@onflow/frw-cadence": "workspace:*",
    "@onflow/frw-context": "workspace:*",
    "@onflow/frw-types": "workspace:*",
    "@onflow/frw-utils": "workspace:*",
    "@onflow/frw-workflow": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "vitest": "^3.2.4"
  },
  "publishConfig": {
    "access": "public"
  }
}
