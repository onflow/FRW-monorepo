import { describe, it, expect } from 'vitest';

describe('@onflow/frw-screens', () => {
  it('should export screens module', () => {
    // Basic test to ensure the package exports work
    expect(true).toBe(true);
  });
});
