{
  "name": "@onflow/frw-context",
  "version": "0.0.1",
  "description": "Dependency injection container and core interfaces for FRW",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "source": "./src/index.ts",
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "build:watch": "tsup --watch",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist",
    "test": "vitest",
    "test:run": "vitest --run",
    "test:coverage": "vitest --coverage",
    "prepublishOnly": "pnpm build",
    "lint": "eslint . --cache",
    "lint:fix": "eslint . --cache --fix"
  },
  "exports": {
    ".": {
      "@onflow/source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "@babel/runtime": "^7.28.2",
    "@onflow/frw-ui": "workspace:*",
    "@onflow/frw-api": "workspace:*",
    "@onflow/frw-cadence": "workspace:*",
    "@onflow/frw-types": "workspace:*",
    "@onflow/frw-utils": "workspace:*",
    "@tanstack/query-core": "^5.85.6"
  },
  "devDependencies": {
    "@types/node": "^22.11.0",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "vitest": "^3.2.4"
  },
  "publishConfig": {
    "access": "public"
  }
}
